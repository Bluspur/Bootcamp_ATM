package gateshead.bluspur.terminalApp;

/**
 * An enum representing the possible outcomes of a transaction.
 * @author craig
 */
public enum TransactionOutcome {
    SUCCESS,
    INSUFFICIENT_FUNDS,
    INSUFFICIENT_PRIVILEGES,
}
