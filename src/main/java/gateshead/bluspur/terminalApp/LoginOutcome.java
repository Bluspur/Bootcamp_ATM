package gateshead.bluspur.terminalApp;

/**
 * An enum representing the outcome of a login attempt.
 * @author craig
 */
public enum LoginOutcome {
    SUCCESS,
    USER_NOT_FOUND,
    INCORRECT_PASSWORD,
}
